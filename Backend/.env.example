# MongoDB – required for backend. Default: local MongoDB. For Atlas use your connection string.
# MONGODB_URI=mongodb://localhost:27017/titoubarz
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority

# PayPal - https://developer.paypal.com/dashboard/applications/sandbox
# Use Sandbox credentials for testing, Live for production
PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_CLIENT_SECRET=your_paypal_client_secret
# Set to "production" for live payments
PAYPAL_MODE=sandbox

# OpenRouter (AI) - https://openrouter.ai/keys
# Required for AI-enhanced program generation (1-week + 6-week). Get free key at openrouter.ai
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxx
# More stable: openai/gpt-4o-mini. Free but rate-limited: openai/gpt-oss-120b:free (app will retry on 429)
OPENROUTER_MODEL=openai/gpt-4o-mini

# Brevo (email) - https://app.brevo.com/settings/keys/api
BREVO_API_KEY=your_brevo_api_key
# Sender email must be verified in Brevo: Settings > Senders
BREVO_SENDER_EMAIL=your-verified-email@yourdomain.com
BREVO_SENDER_NAME=Toufik Calisthenics
